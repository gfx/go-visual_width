// THIS FILE IS AUTOMATICALLY GENERATED.
// DO NOT EDIT IT!
//
// Unicode version 7.0.0 of EastAsianWidth characters
//
// see http://www.unicode.org/reports/tr11/ for specification
// see http://www.unicode.org/Public/UNIDATA/EastAsianWidth.txt for data

package visual_width
const wide = `[` +
  `\x{1100}-\x{115F}` +
  `\x{2329}-\x{232A}` +
  `\x{2E80}-\x{2FFF}` +
  `\x{3001}-\x{303E}` +
  `\x{3041}-\x{3247}` +
  `\x{3250}-\x{4DBF}` +
  `\x{4E00}-\x{A4CF}` +
  `\x{A960}-\x{A97F}` +
  `\x{AC00}-\x{D7AF}` +
  `\x{F900}-\x{FAFF}` +
  `\x{FE10}-\x{FE1F}` +
  `\x{FE30}-\x{FE6F}` +
  `\x{1B000}-\x{1BBFF}` +
  `\x{1F200}-\x{1F2FF}` +
  `\x{20000}-\x{E0000}` +
  `]`

const fullwide = `[` +
  `\x{3000}` +
  `\x{FF01}-\x{FF60}` +
  `\x{FFE0}-\x{FFE7}` +
  `]`

const ambiguous = `[` +
  `\x{00A1}` +
  `\x{00A4}` +
  `\x{00A7}-\x{00A8}` +
  `\x{00AA}` +
  `\x{00AD}-\x{00AE}` +
  `\x{00B0}-\x{00B4}` +
  `\x{00B6}-\x{00BA}` +
  `\x{00BC}-\x{00BF}` +
  `\x{00C6}` +
  `\x{00D0}` +
  `\x{00D7}-\x{00D8}` +
  `\x{00DE}-\x{00E1}` +
  `\x{00E6}` +
  `\x{00E8}-\x{00EA}` +
  `\x{00EC}-\x{00ED}` +
  `\x{00F0}` +
  `\x{00F2}-\x{00F3}` +
  `\x{00F7}-\x{00FA}` +
  `\x{00FC}` +
  `\x{00FE}` +
  `\x{0101}` +
  `\x{0111}` +
  `\x{0113}` +
  `\x{011B}` +
  `\x{0126}-\x{0127}` +
  `\x{012B}` +
  `\x{0131}-\x{0133}` +
  `\x{0138}` +
  `\x{013F}-\x{0142}` +
  `\x{0144}` +
  `\x{0148}-\x{014B}` +
  `\x{014D}` +
  `\x{0152}-\x{0153}` +
  `\x{0166}-\x{0167}` +
  `\x{016B}` +
  `\x{01CE}` +
  `\x{01D0}` +
  `\x{01D2}` +
  `\x{01D4}` +
  `\x{01D6}` +
  `\x{01D8}` +
  `\x{01DA}` +
  `\x{01DC}` +
  `\x{0251}` +
  `\x{0261}` +
  `\x{02C4}` +
  `\x{02C7}` +
  `\x{02C9}-\x{02CB}` +
  `\x{02CD}` +
  `\x{02D0}` +
  `\x{02D8}-\x{02DB}` +
  `\x{02DD}` +
  `\x{02DF}` +
  `\x{0300}-\x{036F}` +
  `\x{0391}-\x{03A9}` +
  `\x{03B1}-\x{03C1}` +
  `\x{03C3}-\x{03C9}` +
  `\x{0401}` +
  `\x{0410}-\x{044F}` +
  `\x{0451}` +
  `\x{2010}` +
  `\x{2013}-\x{2016}` +
  `\x{2018}-\x{2019}` +
  `\x{201C}-\x{201D}` +
  `\x{2020}-\x{2022}` +
  `\x{2024}-\x{2027}` +
  `\x{2030}` +
  `\x{2032}-\x{2033}` +
  `\x{2035}` +
  `\x{203B}` +
  `\x{203E}` +
  `\x{2074}` +
  `\x{207F}` +
  `\x{2081}-\x{2084}` +
  `\x{20AC}` +
  `\x{2103}` +
  `\x{2105}` +
  `\x{2109}` +
  `\x{2113}` +
  `\x{2116}` +
  `\x{2121}-\x{2122}` +
  `\x{2126}` +
  `\x{212B}` +
  `\x{2153}-\x{2154}` +
  `\x{215B}-\x{215E}` +
  `\x{2160}-\x{216B}` +
  `\x{2170}-\x{2179}` +
  `\x{2189}-\x{2199}` +
  `\x{21B8}-\x{21B9}` +
  `\x{21D2}` +
  `\x{21D4}` +
  `\x{21E7}` +
  `\x{2200}` +
  `\x{2202}-\x{2203}` +
  `\x{2207}-\x{2208}` +
  `\x{220B}` +
  `\x{220F}` +
  `\x{2211}` +
  `\x{2215}` +
  `\x{221A}` +
  `\x{221D}-\x{2220}` +
  `\x{2223}` +
  `\x{2225}` +
  `\x{2227}-\x{222C}` +
  `\x{222E}` +
  `\x{2234}-\x{2237}` +
  `\x{223C}-\x{223D}` +
  `\x{2248}` +
  `\x{224C}` +
  `\x{2252}` +
  `\x{2260}-\x{2261}` +
  `\x{2264}-\x{2267}` +
  `\x{226A}-\x{226B}` +
  `\x{226E}-\x{226F}` +
  `\x{2282}-\x{2283}` +
  `\x{2286}-\x{2287}` +
  `\x{2295}` +
  `\x{2299}` +
  `\x{22A5}` +
  `\x{22BF}` +
  `\x{2312}` +
  `\x{2460}-\x{24E9}` +
  `\x{24EB}-\x{254B}` +
  `\x{2550}-\x{2573}` +
  `\x{2580}-\x{258F}` +
  `\x{2592}-\x{2595}` +
  `\x{25A0}-\x{25A1}` +
  `\x{25A3}-\x{25A9}` +
  `\x{25B2}-\x{25B3}` +
  `\x{25B6}-\x{25B7}` +
  `\x{25BC}-\x{25BD}` +
  `\x{25C0}-\x{25C1}` +
  `\x{25C6}-\x{25C8}` +
  `\x{25CB}` +
  `\x{25CE}-\x{25D1}` +
  `\x{25E2}-\x{25E5}` +
  `\x{25EF}` +
  `\x{2605}-\x{2606}` +
  `\x{2609}` +
  `\x{260E}-\x{260F}` +
  `\x{2614}-\x{2615}` +
  `\x{261C}` +
  `\x{261E}` +
  `\x{2640}` +
  `\x{2642}` +
  `\x{2660}-\x{2661}` +
  `\x{2663}-\x{2665}` +
  `\x{2667}-\x{266A}` +
  `\x{266C}-\x{266D}` +
  `\x{266F}` +
  `\x{269E}-\x{269F}` +
  `\x{26BE}-\x{26BF}` +
  `\x{26C4}-\x{26CD}` +
  `\x{26CF}-\x{26E1}` +
  `\x{26E3}` +
  `\x{26E8}-\x{26FF}` +
  `\x{273D}` +
  `\x{2757}` +
  `\x{2776}-\x{277F}` +
  `\x{2B55}-\x{2B59}` +
  `\x{3248}-\x{324F}` +
  `\x{E000}-\x{F8FF}` +
  `\x{FE00}-\x{FE0F}` +
  `\x{FFFD}-\x{FFFF}` +
  `\x{1F100}-\x{1F10A}` +
  `\x{1F110}-\x{1F12D}` +
  `\x{1F130}-\x{1F169}` +
  `\x{1F170}-\x{1F1E5}` +
  `]`

